<body class="bg-light">
    <div class="d-flex flex-column align-items-center justify-content-start min-vh-100 px-3 py-4 overflow-auto">
        <!-- Mensagens de Erro -->
        {{#if error}}
            <div class="alert alert-danger w-100" style="max-width: 500px;">{{error}}</div>
        {{/if}}
        
        <!-- Formulário -->
        <form class="p-4 shadow rounded bg-white" style="max-width: 500px; width: 100%;" action="/user/login" method="post">
            <h2 class="text-center mb-4">Página de Login</h2>
            <div class="form-group mt-2">
                <label for="email" class="fs-5">Email</label>
                <input type="email" class="form-control mt-2" id="email" name="email" required>
                <div id="emailHelper" class="form-text">Ex: abcd@email.com</div>
            </div>
            <div class="form-group mt-2 position-relative">
                <label for="senha" class="fs-5">Senha</label>
                <div class="input-group mt-2">
                <input type="password" class="form-control" name="senha" id="senha" required>
                <button type="button" class="btn btn-outline-secondary" onclick="toggleSenha('senha', this)">
                    <i class="bi bi-eye"></i>
                </button>
                </div>
                <div id="passwordHelper" class="form-text">
                Sua senha deve ter de 8-20 caracteres, conter pelo menos uma letra maiúscula, um número e um símbolo: !@#$%*
                </div>
            </div>
            <button type="submit" class="btn btn-primary w-100 mt-4">Login</button>
            <br>
        </form>
    </div>
</body>
